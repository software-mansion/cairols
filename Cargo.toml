[package]
name = "cairo-language-server"
version = "2.9.2"
edition = "2021"

authors = ["Software Mansion <contact@swmansion.com>", "StarkWare <info@starkware.co>"]
description = "The Cairo Language Server"
license = "Apache-2.0"
repository = "https://github.com/software-mansion/cairols"

[workspace]
members = ["xtask"]

[features]
testing = []

# Managing dependencies on crates from starkware-libs/cairo repository:
#
# The Cairo compiler is made of a bunch of crates that inter-depend on each other and have
# synchronised versioning.
# It is very important to use a single revision of these crates in the entire Cairo toolchain,
# which consists of Cairo compiler, Scarb, CairoLS and other tools.
# The toolchain is eventually built by Scarb, which depends on everything other as regular crates.
# To ensure that all crates in the toolchain use the same revision of Cairo crates, we use a patch
# mechanism that Cargo provides.
# Because Cargo requires patches to change the crate source, we have an unspoken contract that
# all tools *always* depend on some crates.io versions of Cairo crates and Scarb uses
# [patch.crates.io] table to set final git revision for everything.
#
# To keep our Cargo.toml following this contract, always use `cargo xtask upgrade`
# for manipulating these dependencies.
[dependencies]
anyhow = "1"
cairo-lang-compiler = "*"
cairo-lang-defs = "*"
cairo-lang-diagnostics = "*"
cairo-lang-doc = "*"
cairo-lang-executable = "*"
cairo-lang-filesystem = "*"
cairo-lang-formatter = "*"
cairo-lang-lowering = "*"
cairo-lang-macro = "0.1.1"
cairo-lang-parser = "*"
cairo-lang-project = "*"
cairo-lang-semantic = "*"
cairo-lang-starknet = "*"
cairo-lang-syntax = "*"
cairo-lang-syntax-codegen = "*"
cairo-lang-test-plugin = "*"
cairo-lang-utils = "*"
colored = "3.0.0"
convert_case = "0.7.1"
crossbeam = "0.8.4"
governor = { version = "0.8.0", default-features = false, features = ["std", "quanta"] }
if_chain = "1"
indent = "0.1.1"
indoc = "2"
itertools = "0.14.0"
jod-thread = "0.1.2"
lsp-server = "0.7.8"
lsp-types = "=0.95.1"
memchr = "2.7.4"
salsa = { package = "rust-analyzer-salsa", version = "0.17.0-pre.6" }
scarb-metadata = "1.13"
scarb-proc-macro-server-types = "0.1"
scarb-stable-hash = "1"
serde = { version = "1", default-features = false, features = ["derive"] }
serde_json = "1"
smol_str = { version = "0.3.2", features = ["serde"] }
tempfile = "3"
toml = "0.8"
tracing = "0.1"
tracing-chrome = "0.7.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
which = "7"

[target.'cfg(target_vendor = "apple")'.dependencies]
libc = "0.2.169"

[dev-dependencies]
assert_fs = "1.1"
cairo-lang-test-utils = { version = "*", features = ["testing"] }
cairo-language-server = { path = ".", features = ["testing"] }
insta = { version = "1.42", features = ["toml"] }
pathdiff = "0.2"
similar = "2.7"
test-log = "0.2.16"

# Here we specify real dependency specifications for Cairo crates *if* currently we want to use
# a particular unreleased commit (which is frequent mid-development).
# We list all Cairo crates that go into CairoLS's compilation unit even if LS itself does not depend
# on some of them directly.
# This ensures no duplicate instances of Cairo crates are pulled in by mistake.
[patch.crates-io]
cairo-lang-casm = { path = "../../starkware-libs/cairo/crates/cairo-lang-casm" }
cairo-lang-compiler = { path = "../../starkware-libs/cairo/crates/cairo-lang-compiler" }
cairo-lang-debug = { path = "../../starkware-libs/cairo/crates/cairo-lang-debug" }
cairo-lang-defs = { path = "../../starkware-libs/cairo/crates/cairo-lang-defs" }
cairo-lang-diagnostics = { path = "../../starkware-libs/cairo/crates/cairo-lang-diagnostics" }
cairo-lang-doc = { path = "../../starkware-libs/cairo/crates/cairo-lang-doc" }
cairo-lang-eq-solver = { path = "../../starkware-libs/cairo/crates/cairo-lang-eq-solver" }
cairo-lang-executable = { path = "../../starkware-libs/cairo/crates/cairo-lang-executable" }
cairo-lang-filesystem = { path = "../../starkware-libs/cairo/crates/cairo-lang-filesystem" }
cairo-lang-formatter = { path = "../../starkware-libs/cairo/crates/cairo-lang-formatter" }
cairo-lang-lowering = { path = "../../starkware-libs/cairo/crates/cairo-lang-lowering" }
cairo-lang-parser = { path = "../../starkware-libs/cairo/crates/cairo-lang-parser" }
cairo-lang-plugins = { path = "../../starkware-libs/cairo/crates/cairo-lang-plugins" }
cairo-lang-proc-macros = { path = "../../starkware-libs/cairo/crates/cairo-lang-proc-macros" }
cairo-lang-project = { path = "../../starkware-libs/cairo/crates/cairo-lang-project" }
cairo-lang-runnable-utils = { path = "../../starkware-libs/cairo/crates/cairo-lang-runnable-utils" }
cairo-lang-semantic = { path = "../../starkware-libs/cairo/crates/cairo-lang-semantic" }
cairo-lang-sierra = { path = "../../starkware-libs/cairo/crates/cairo-lang-sierra" }
cairo-lang-sierra-ap-change = { path = "../../starkware-libs/cairo/crates/cairo-lang-sierra-ap-change" }
cairo-lang-sierra-gas = { path = "../../starkware-libs/cairo/crates/cairo-lang-sierra-gas" }
cairo-lang-sierra-generator = { path = "../../starkware-libs/cairo/crates/cairo-lang-sierra-generator" }
cairo-lang-sierra-to-casm = { path = "../../starkware-libs/cairo/crates/cairo-lang-sierra-to-casm" }
cairo-lang-sierra-type-size = { path = "../../starkware-libs/cairo/crates/cairo-lang-sierra-type-size" }
cairo-lang-starknet = { path = "../../starkware-libs/cairo/crates/cairo-lang-starknet" }
cairo-lang-starknet-classes = { path = "../../starkware-libs/cairo/crates/cairo-lang-starknet-classes" }
cairo-lang-syntax = { path = "../../starkware-libs/cairo/crates/cairo-lang-syntax" }
cairo-lang-syntax-codegen = { path = "../../starkware-libs/cairo/crates/cairo-lang-syntax-codegen" }
cairo-lang-test-plugin = { path = "../../starkware-libs/cairo/crates/cairo-lang-test-plugin" }
cairo-lang-test-utils = { path = "../../starkware-libs/cairo/crates/cairo-lang-test-utils" }
cairo-lang-utils = { path = "../../starkware-libs/cairo/crates/cairo-lang-utils" }

# The profile used for CI in pull requests.
# External dependencies are built with optimisation enabled,
# while crates in this workspace are built with `dev` profile and full debug info.
# This is a trade-off between build time and e2e test time.
[profile.ci]
inherits = "test"
[profile.ci.package."*"] # external dependencies
opt-level = 1
[profile.ci.package."rust-analyzer-salsa"]
opt-level = 3

[profile.dev.package]
insta.opt-level = 3
similar.opt-level = 3
